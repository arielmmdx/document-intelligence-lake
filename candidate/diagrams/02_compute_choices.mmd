%% 02 — Compute choices you must design. Paste into https://mermaid.live
%% Layout/contract first (sample). Scale extract second. Airflow orchestrates both.
flowchart TB
  AF[Apache Airflow on EC2]
  AF --> Q{What did we land?}

  Q -->|PDF| PAGES[Sample pages for a contract then split the rest across workers]
  Q -->|Excel or CSV| SPARK[Sample headers/sheets for a contract then PySpark on the rest]
  Q -->|Word| WORD[Sample headings/tables for a contract then ECS Python]
  Q -->|SQLite| PY[Is the SQL schema already the contract?]
  Q -->|unreadable| QZ[Quarantine — fail closed]
